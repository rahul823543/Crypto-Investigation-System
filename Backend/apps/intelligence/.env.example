# Intelligence Service — Environment Variables
# Copy this file to .env and fill in your values

# Port the FastAPI service listens on
INTELLIGENCE_PORT=8001

# Semantic version string echoed in every API response
ENGINE_VERSION=0.1.0

# CORS: comma-separated allowed origins (use * for local dev only)
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173
